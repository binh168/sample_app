<li>
  <%= gravatar_for user, size: Settings.gravatar2 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && current_user.present?(user) %>
  | <%= link_to t(".delete"), user, method: :delete, data: { confirm: t(".sure") } %>
  <% end %>
</li>